const urlRoot = '/project-4/NetflixClone'

export const HOME = `${urlRoot}/index.html`;
export const BROWSE = `${urlRoot}/browse`;
export const SIGN_UP = `${urlRoot}/signup`;
export const SIGN_IN = `${urlRoot}/signin`;